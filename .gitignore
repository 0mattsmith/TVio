node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store
Screenshot*.png

# Tauri (Rust build output + generated files)
src-tauri/target
src-tauri/gen

# Capacitor (generated native projects — regenerated in CI / via `npx cap add`)
/android
/ios
.gradle

# Secrets — never commit service account keys or signing keys
*firebase-adminsdk*.json
*serviceAccount*.json
*.pem
*.key
*.keystore
*.jks

# Generated icon source images (written by scripts/gen-android-assets.mjs in CI)
/assets/

# Misc
*.apk
*.msi
*.exe
